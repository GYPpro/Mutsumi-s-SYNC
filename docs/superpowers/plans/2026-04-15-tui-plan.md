# 窗口化 TUI 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将流式日志 TUI 重构为窗口化 TUI，实现多对话管理和交互式详情查看

**Architecture:** 使用 Textual 框架构建窗口化界面，对话数据存储在内存中，日志写入文件。状态栏显示 AI API、NapCat 和消息计数。

**Tech Stack:** Textual, httpx, logging

---

## Task 1: 创建 TUI 模块基础结构

**Files:**
- Create: `src/mutsumi_sync/tui/__init__.py`
- Create: `src/mutsumi_sync/tui/storage.py` - 对话数据存储
- Modify: `requirements.txt` - 添加 textual 依赖

- [ ] **Step 1: 创建 tui 模块目录和 __init__.py**

```python
# src/mutsumi_sync/tui/__init__.py
"""Mutsumi's SYNC TUI 模块"""

from .storage import ConversationStorage, Conversation, Round, ToolCall

__all__ = ["ConversationStorage", "Conversation", "Round", "ToolCall"]
```

- [ ] **Step 2: 创建对话数据存储**

```python
# src/mutsumi_sync/tui/storage.py
from dataclasses import dataclass, field
from typing import Optional
from datetime import datetime

@dataclass
class ToolCall:
    name: str
    args: dict
    result: str = ""

@dataclass
class Round:
    timestamp: str
    user_message: str
    bot_message: str
    tool_calls: list[ToolCall] = field(default_factory=list)

@dataclass
class Conversation:
    peer_id: str
    peer_name: str = ""
    chat_type: int = 1
    rounds: list[Round] = field(default_factory=list)
    last_active: str = ""

class ConversationStorage:
    def __init__(self):
        self.conversations: dict[str, Conversation] = {}
        self.message_count: int = 0
    
    def add_message(self, peer_id: str, peer_name: str, chat_type: int, 
                    user_msg: str, bot_msg: str, tool_calls: list[ToolCall] = None):
        if peer_id not in self.conversations:
            self.conversations[peer_id] = Conversation(
                peer_id=peer_id,
                peer_name=peer_name,
                chat_type=chat_type
            )
        conv = self.conversations[peer_id]
        round = Round(
            timestamp=datetime.now().strftime("%H:%M:%S"),
            user_message=user_msg,
            bot_message=bot_msg,
            tool_calls=tool_calls or []
        )
        conv.rounds.append(round)
        conv.last_active = round.timestamp
        self.message_count += 1
    
    def get_conversations(self) -> list[Conversation]:
        return list(self.conversations.values())
    
    def get_conversation(self, peer_id: str) -> Optional[Conversation]:
        return self.conversations.get(peer_id)
```

- [ ] **Step 3: 添加 textual 到 requirements.txt**

在 requirements.txt 中添加 `textual>=0.40.0`

---

## Task 2: 创建 Textual 主应用

**Files:**
- Create: `src/mutsumi_sync/tui/app.py`

- [ ] **Step 1: 创建 Textual 应用基础结构**

```python
# src/mutsumi_sync/tui/app.py
from textual.app import App, ComposeResult
from textual.widgets import Static, Footer
from textual.containers import Container, Horizontal, Vertical
from textual.binding import Binding
from .storage import ConversationStorage, ToolCall

class MutsumiTUI(App):
    CSS = """
    Screen { height: 100%; }
    # header { height: 3; background: $primary; }
    # content { height: 1fr; }
    # status { height: 3; background: $surface; }
    """
    
    BINDINGS = [
        Binding("left", "prev_user", "上一用户"),
        Binding("right", "next_user", "下一用户"),
        Binding("up", "prev_round", "上一轮"),
        Binding("down", "next_round", "下一轮"),
        Binding("enter", "show_detail", "详情"),
        Binding("escape", "back", "返回"),
    ]
    
    def __init__(self, storage: ConversationStorage):
        super().__init__()
        self.storage = storage
        self.current_user_idx = 0
        self.current_round_idx = 0
    
    def compose(self) -> ComposeResult:
        yield Static("Mutsumi's SYNC", id="header")
        with Vertical(id="content"):
            pass
        with Horizontal(id="status"):
            yield Static("初始化中...", id="status_text")
    
    def action_next_user(self):
        convs = self.storage.get_conversations()
        if convs:
            self.current_user_idx = (self.current_user_idx + 1) % len(convs)
            self.current_round_idx = 0
            self.refresh_content()
    
    def action_prev_user(self):
        convs = self.storage.get_conversations()
        if convs:
            self.current_user_idx = (self.current_user_idx - 1) % len(convs)
            self.current_round_idx = 0
            self.refresh_content()
    
    def action_next_round(self):
        convs = self.storage.get_conversations()
        if convs and convs[self.current_user_idx].rounds:
            max_idx = len(convs[self.current_user_idx].rounds) - 1
            self.current_round_idx = min(self.current_round_idx + 1, max_idx)
            self.refresh_content()
    
    def action_prev_round(self):
        self.current_round_idx = max(self.current_round_idx - 1, 0)
        self.refresh_content()
    
    def refresh_content(self):
        # 更新内容显示
        pass
    
    def update_status(self, ai_online: bool, napcat_online: bool, msg_count: int):
        ai_status = "●" if ai_online else "○"
        nc_status = "●" if napcat_online else "○"
        status = f"[AI: {ai_status}] [NapCat: {nc_status}] [消息: {msg_count}]"
        self.query_one("#status_text", Static).update(status)
```

---

## Task 3: 创建主屏幕和状态栏

**Files:**
- Modify: `src/mutsumi_sync/tui/app.py`
- Create: `src/mutsumi_sync/tui/widgets/__init__.py`

- [ ] **Step 1: 完善主屏幕布局**

添加用户标签显示和对话内容区域

- [ ] **Step 2: 创建状态栏组件**

```python
# src/mutsumi_sync/tui/widgets/status_bar.py
from textual.widget import Widget
from textual.widgets import Static

class StatusBar(Widget):
    def __init__(self, ai_online: bool = False, napcat_online: bool = False, message_count: int = 0):
        super().__init__()
        self.ai_online = ai_online
        self.napcat_online = napcat_online
        self.message_count = message_count
    
    def compose(self):
        yield Static(self._get_status_text(), id="status")
    
    def _get_status_text(self) -> str:
        ai = "●" if self.ai_online else "○"
        nc = "●" if self.napcat_online else "○"
        return f"[AI: {ai}] [NapCat: {nc}] [消息: {self.message_count}]  [←→切换用户 ↑↓切换轮次 Enter详情]"
    
    def update(self, ai_online: bool, napcat_online: bool, message_count: int):
        self.ai_online = ai_online
        self.napcat_online = napcat_online
        self.message_count = message_count
        self.query_one("#status", Static).update(self._get_status_text())
```

- [ ] **Step 3: 创建对话组件**

```python
# src/mutsumi_sync/tui/widgets/conversation.py
from textual.widget import Widget
from textual.widgets import Static
from ..storage import Conversation, Round

class ConversationView(Widget):
    def __init__(self, conversation: Conversation = None, round_idx: int = 0):
        super().__init__()
        self.conversation = conversation
        self.round_idx = round_idx
    
    def compose(self):
        if not self.conversation:
            yield Static("暂无对话")
            return
        
        conv = self.conversation
        if not conv.rounds:
            yield Static(f"用户 {conv.peer_id} 暂无对话记录")
            return
        
        if self.round_idx >= len(conv.rounds):
            self.round_idx = 0
        
        round = conv.rounds[self.round_idx]
        content = f"""第 {self.round_idx + 1}/{len(conv.rounds)} 轮对话

{round.timestamp} 用户: {round.user_message}

{round.timestamp} Bot: {round.bot_message}"""
        
        yield Static(content, id="conversation")
    
    def update(self, conversation: Conversation, round_idx: int):
        self.conversation = conversation
        self.round_idx = round_idx
        self.query_one("#conversation", Static).update(self._build_content())
    
    def _build_content(self) -> str:
        if not self.conversation or not self.conversation.rounds:
            return "暂无对话"
        
        round = self.conversation.rounds[self.round_idx]
        return f"""第 {self.round_idx + 1}/{len(self.conversation.rounds)} 轮对话

{round.timestamp} 用户: {round.user_message}

{round.timestamp} Bot: {round.bot_message}"""
```

---

## Task 4: 创建详情屏幕

**Files:**
- Create: `src/mutsumi_sync/tui/screens/detail.py`

- [ ] **Step 1: 创建详情屏幕**

```python
# src/mutsumi_sync/tui/screens/detail.py
from textual.app import ComposeResult
from textual.screen import Screen
from textual.widgets import Static, Button
from textual.containers import Vertical, Horizontal
from ..storage import Conversation, Round

class DetailScreen(Screen):
    def __init__(self, conversation: Conversation, round_idx: int):
        super().__init__()
        self.conversation = conversation
        self.round_idx = round_idx
    
    def compose(self) -> ComposeResult:
        round = self.conversation.rounds[self.round_idx]
        
        yield Vertical(
            Static(f"对话详情 - 用户 {self.conversation.peer_id} 第 {self.round_idx + 1} 轮", id="detail_header"),
            Static(f"用户消息: {round.user_message}", id="user_msg"),
            Static(f"Bot 回复: {round.bot_message}", id="bot_msg"),
            id="detail_content"
        )
        
        if round.tool_calls:
            tool_info = "--- Tool Calls ---\n"
            for i, tc in enumerate(round.tool_calls, 1):
                tool_info += f"[{i}] {tc.name}\n"
                for k, v in tc.args.items():
                    tool_info += f"    {k}: {v}\n"
                if tc.result:
                    tool_info += f"    结果: {tc.result}\n"
            yield Static(tool_info, id="tool_calls")
        
        yield Horizontal(
            Button("返回", id="back_btn"),
            id="detail_footer"
        )
    
    def on_button_pressed(self, event: Button.Pressed):
        if event.button.id == "back_btn":
            self.app.pop_screen()
```

- [ ] **Step 2: 在主应用中集成详情屏幕**

修改 app.py 添加 show_detail 和 back 操作

---

## Task 5: 集成到 start_tui.py

**Files:**
- Modify: `start_tui.py` - 重构为 Textual 应用启动

- [ ] **Step 1: 重构启动脚本**

```python
#!/usr/bin/env python3
"""Mutsumi's SYNC - 窗口化 TUI"""
import asyncio
import logging
from pathlib import Path
from textual.driver import Driver

sys.path.insert(0, str(Path(__file__).parent))

from src.mutsumi_sync.config import load_config
from src.mutsumi_sync.bot import MutsumiSync
from src.mutsumi_sync.tui.app import MutsumiTUI
from src.mutsumi_sync.tui.storage import ConversationStorage, ToolCall

logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s [%(levelname)s] %(message)s',
    handlers=[
        logging.FileHandler('./logs/mutsumi.log'),
        logging.NullHandler()
    ]
)

class BotWithTUI:
    def __init__(self, config, tui_app: MutsumiTUI):
        self.config = config
        self.tui = tui_app
        self.bot = MutsumiSync(config, log_callback=self.on_bot_message)
    
    async def run(self):
        # 启动 TUI
        self.tui.run()
        # 同时启动 bot
        await self.bot.run()
    
    def on_bot_message(self, msg: str):
        # 解析消息，提取对话信息并更新 TUI
        # 从日志中提取 peer_id, user_msg, bot_msg, tool_calls
        self.tui.storage.add_message(peer_id, peer_name, chat_type, user_msg, bot_msg, tool_calls)

async def main():
    config = load_config()
    storage = ConversationStorage()
    tui_app = MutsumiTUI(storage)
    
    bot_with_tui = BotWithTUI(config, tui_app)
    await bot_with_tui.run()

if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: 运行测试**

---

## Task 6: 状态检查功能

**Files:**
- Modify: `src/mutsumi_sync/tui/app.py` - 添加定时状态检查

- [ ] **Step 1: 实现 API 状态检查**

```python
async def check_api_status(self):
    import httpx
    
    # 检查 AI API
    try:
        async with httpx.AsyncClient() as client:
            resp = await client.get(f"{self.config.model.base_url}/models", timeout=5)
            ai_online = resp.status_code in (200, 401)
    except:
        ai_online = False
    
    # 检查 NapCat
    napcat_online = self.bot.receiver._ws is not None
    
    self.update_status(ai_online, napcat_online, self.storage.message_count)
```

- [ ] **Step 2: 添加定时任务**

使用 `asyncio.create_task` 定期调用检查函数

---

## 执行方式

**Plan complete and saved to `docs/superpowers/plans/2026-04-15-tui-plan.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**